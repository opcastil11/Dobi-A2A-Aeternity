export default function genShortUIntConstField<Value extends number, Optional extends boolean = false>(constValue: Value, optional?: Optional): {
    serialize: Optional extends true ? (value?: Value) => Buffer : (value: Value) => Buffer;
    deserialize: (value: Buffer) => Value;
    constValue: Value;
    constValueOptional: boolean;
};
