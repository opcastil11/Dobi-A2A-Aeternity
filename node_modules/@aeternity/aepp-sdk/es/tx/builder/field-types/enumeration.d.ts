export default function genEnumerationField<Enum extends {
    [key: string]: number | string;
}>(enm: Enum): {
    serialize: (value: Enum[keyof Enum]) => Buffer;
    deserialize: (value: Buffer) => Enum[keyof Enum];
};
