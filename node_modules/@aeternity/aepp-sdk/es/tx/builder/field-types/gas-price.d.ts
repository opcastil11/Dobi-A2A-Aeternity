import { Int } from '../constants.js';
import Node from '../../../Node.js';
import { AE_AMOUNT_FORMATS } from '../../../utils/amount-formatter.js';
export declare function getCachedIncreasedGasPrice(node: Node): Promise<bigint>;
declare const _default: {
    prepare(value: Int | undefined, params: {}, { onNode, denomination, }: {
        onNode?: Node;
        /**
         * @deprecated no replacement implemented yet
         */
        denomination?: AE_AMOUNT_FORMATS;
    }): Promise<Int | undefined>;
    serializeAettos(value?: string | undefined): string;
    serialize(value: Int | undefined, params: {}, { denomination, ...options }: {
        denomination?: AE_AMOUNT_FORMATS;
    }): Buffer;
    deserialize(value: Buffer): string;
};
export default _default;
